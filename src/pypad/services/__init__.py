"""Mark this directory as a Python package and describe the role of the package in the larger application.

This module belongs to the shared service layer that supports multiple UI workflows. It helps explain how `pypad.services.__init__` is structured and where this file fits into the runtime workflow.
"""

